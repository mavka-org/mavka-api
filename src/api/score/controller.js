export const score = (req, res) => {
  // ToDO
  // const { username, password } = req.body
  // authorize(username, password)
  //   .then(unauthorized(res))
  //   .then(function (isAuthorized) {
  //     if (isAuthorized) {
  //       return Job.create(req.body)
  //     }
  //     return null
  //   })
  //   .then(internalError(res))
  //   .then(success(res))
  return null
}
